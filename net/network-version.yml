---
- hosts: network
  connection: network_cli
  gather_facts: no
  vars:
    minimum_version_list:
      ios: "{{ tower_version_value | default('16.06.01')}}"
      junos: "{{ tower_version_value | default('17.2R1.13')}}"

  tasks:
     - include_role:
         name: network_version
